
name: deployment

on:
  push:
      branches: ["main"]

      permissions:
        contents: read
        pages: write
        id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: pages-deployment
        uses: actions/deploy-pages@v4
